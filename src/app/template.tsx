import React from 'react'

const Template = ({children} : {children: React.ReactNode}) => {

  return (
    <div>
      {/* <h1>template</h1> */}
      {children}
    </div>
  )
}

export default Template